export interface MultiFilterSearch {
    termino: string;
    campos: string[];
}

/* import { Paginacion } from "../generales.model";

export class MultiFilterSearch extends Paginacion{
    termino: string;
    campos: string[];
    constructor(termino?:string,campos?:string[]) {
        super();
        this.termino = termino;
        this.campos = campos;
    }
}
 */

